{
  "name": "az400-github-actions-lab",
  "version": "1.0.0",
  "private": true,
  "description": "A compact AZ-400 lab for GitHub Actions, CI, Docker, and Azure deployment.",
  "scripts": {
    "dev": "node --env-file=.env src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check test/server.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
